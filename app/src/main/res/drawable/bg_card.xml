<?xml version="1.0" encoding="utf-8"?>
<!--
  卡片底色的**第一档：未开始**。App 赛程页和小组件都用它。

  ── 三档底色是怎么来的 ──────────────────────────────────────────────
  同一张卡还有两档，见 bg_card_live.xml（进行中）和 bg_card_done.xml（已结束）。
  三个文件内容完全一样，只有 solid 指向的颜色不同 —— 理由和 bg_pill_f1.xml
  开头那段一样：**RemoteViews 不能运行时着色**，只能切预置 drawable。

  底色承担状态、胶囊（bg_pill_*.xml）承担类别，两者不打架：
  扫一眼先看出「哪张卡刚打完、哪张在打」，再看胶囊认类别。

  ── 色值是怎么定的（别凭感觉改）────────────────────────────────────
  三档之间按 **Lab 色差**拉开，不是按明度差 —— 明度差看着够，实际
  在小卡片上根本分不出来（第一版就是这么翻车的）：
    浅色  未开始 #F8FAFC → 进行中 #E4ECF7 → 已结束 #D2DCEB   相邻 ΔE 7.2 / 6.1
    深色  未开始 #111B38 → 进行中 #161D30 → 已结束 #282A2E   相邻 ΔE 7.5 / 12.7
  （人眼能分辨的阈值约 ΔE 2.3。第一版浅色只做到 3.0，等于白做。）

  深色那三档是**按降饱和程度**分的，不是按明度：
  深色底板本身已经是近黑（#0B122A→#05080F），再往下压没有空间 ——
  第一版的 #0A1024 比底板还暗，卡片看着像挖了个洞。
  所以深色改成「越靠后越灰」，语义一样是「退到后面」。

  ── 改的时候注意 ────────────────────────────────────────────────────
  · 描边承担结构，不要再往卡片里加阴影或第二层背景。
  · 改完把上面那两个 ΔE 重算一遍，别只看颜色「差不多」。
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/bg_card" />
    <corners android:radius="8dp" />
    <stroke
        android:width="1dp"
        android:color="@color/hairline" />
</shape>
