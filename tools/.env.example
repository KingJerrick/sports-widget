# 本地跑 tools/fetch_calendar.py 用的密钥。
#
# 用法：把这份文件复制成 tools/.env，填上自己的 key。
#       tools/.env 已经在 .gitignore 里 —— 那个文件**绝不能提交**，仓库是公开的。
#
# CI 上不用这个文件：那里由 .github/workflows/update-calendar.yml 的 env 段
# 从 GitHub Secrets 注入同名环境变量。环境变量优先于这个文件。
#
# 三个都空着也能跑：f1 / motogp / mlb / lol 四个源不需要 key，
# 缺 key 的那几个会各自失败并在 sources.<cat>.error 里写明原因，不影响其它源。

# football-data.org —— 西甲 + 欧冠
# 注册：https://www.football-data.org/client/register
FOOTBALL_DATA_TOKEN=

# PandaScore —— CS2
# 注册：https://app.pandascore.co/signup
PANDASCORE_TOKEN=

# balldontlie —— NBA
# 注册：https://app.balldontlie.io
# ⚠️ 这个 key 放在 Authorization 头里，但**不加 Bearer 前缀**（PandaScore 才加）
BALLDONTLIE_KEY=
